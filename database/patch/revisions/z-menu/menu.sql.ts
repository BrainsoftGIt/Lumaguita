import {block} from "../../core/updater";

block( module, { identifier: "auth.menu.agenda", flags:[ '@unique' ],
// language=PostgreSQL
} ).sql`
--   insert into 
`;
block( module, { identifier: "auth.menu.agenda", flags:[ '@unique' ]
// language=PostgreSQL
} ).sql`
--   insert into 
`;